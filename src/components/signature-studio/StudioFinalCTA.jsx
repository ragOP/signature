import React from 'react';

const StudioFinalCTA = () => {
  return (
    <section className="studio-section text-center">
      <h2 className="studio-headline mb-6 max-w-2xl mx-auto">
        Ready for a signature that looks premium and stays consistent?
      </h2>
      <p className="studio-subhead mb-8 max-w-lg mx-auto">
        Get 3 options + Signature Blueprint PDF + all formats.
      </p>
      <a href="/signature-cart" className="studio-cta">
        Create My Signature →
      </a>
    </section>
  );
};

export default StudioFinalCTA;
